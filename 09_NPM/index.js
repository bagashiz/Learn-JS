//* Importing a module
// use _ is a standard convention for lodash
import _ from "lodash";

const myArray = [1, 2, 3, 4];
let sum = _.sum(myArray); // use lodash to sum the array

console.log(sum); // 10